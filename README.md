# Ebook2XTX
### 项目更名为Ebook2XTX，原ZipComic2XTC项目已删除，如需请看ZipComic2XTC分支，新版本将在ZipComic2XTC原有基础上新增功能。

### 软件需要安装Python运行环境，会自动安装剩余所需的运行组件，需下载UnRAR.exe（跟py文件放一起）才能运行 或者使用命令打包成单文件：

- 控制台版本：python -m PyInstaller --onefile --console --name Ebook2XTX --add-data "UnRAR.exe;." --collect-all numba --collect-all llvmlite --hidden-import numpy --hidden-import py7zr --hidden-import rarfile ebook2xtx.py

- GUI：python -m PyInstaller --onefile --noconsole --name Ebook2XTX_GUI --add-data "UnRAR.exe;." --collect-all numba --collect-all llvmlite --hidden-import numpy --hidden-import py7zr --hidden-import rarfile ebook2xtx_GUI.py

### 软件功能
- 支持输入的格式：XTC (1-bit 黑白，容器格式) XTCH (2-bit 4级灰度，容器格式) XTG (1-bit 黑白，单页模式) XTH (2-bit 4级灰度，单页模式)，压缩包（zip，rar，7z，cbr，cbz等）格式的图片（jpg，bmp，png，webp等）漫画，文件夹形式的图片漫画（jpg，bmp，png，webp等），电子书格式（pdf，epub，mobi，azw3）的漫画，纯文本/图文混排的电子书格式暂不支持（仅支持导出图片或导出文本为txt文件）。
- 支持转换的格式： XTC (1-bit 黑白，容器格式，单文件) XTCH (2-bit 4级灰度，容器格式，单文件) XTG (1-bit 黑白，单页模式，每个图片单独输出) XTH (2-bit 4级灰度，单页模式，每个图片单独输出)，图片格式（jpg，png，webp，bmp），电子书格式（epub，pdf）

- 可选择转换分辨率： X4（480×800）（默认），X4双倍分辨率（960×1600），X3 （528×792），X3双倍分辨率（1056×1584），原图分辨率，自定义分辨率
- 漫画白边自动裁切： 开（默认）/关
- 画面切割： 横切2图：1:1.618，1.618:1，1:1（其中1:1模式下支持切2图+1中间帧）
- 横切3图：1:2:1，2:1:1，1:1:2，1:1:1（其中1:1:1模式下支持切3图+2中间帧）
- 漫画内横版图片自动旋转： 不旋转 顺时针旋转90度（默认，x4按键在右面） 逆时针90度
- 拉伸至全屏： 开（默认）/关
- 抖动强度： 默认0.7（可调范围0-1）
- 容器格式分包： 4G（FAT32），自定义大小或不分包
- 同时处理的线程数量： 默认为CPU线程数，不建议修改，经测试能跑满CPU
- GIF文件处理：转换第一帧（默认），转换所有帧
- 透明支持：不可修改，默认将透明部分处理成白色（255,255,255），因为大部分墨水屏设备底色为白色
- 输出非容器格式文件名命名格式：编号.格式（默认），书名-编号.格式
- 电子书格式支持自动判断类型：图片（完整支持），文本（支持单独导出为txt），图文混排（仅支持单独导出为txt或单独导出图片，建议先转换成pdf格式再使用本软件）

## 代码由Deepseek生成
### ai提示词：
```
## 1. 总体目标
开发一个 Python 程序，用于将常见电子书、漫画压缩包、图片文件夹以及已有的专有格式文件，转换为 Xteink 设备（X4/X3 等分辨率）支持的 XTC、XTCH、XTG、XTH 格式，同时支持输出通用图片格式（JPEG、PNG、WebP、BMP）和标准电子书格式（EPUB、PDF）。程序提供控制台交互版本（`ebook2xtx.py`）和图形化界面版本（`ebook2xtx_GUI.py`），二者共享核心处理逻辑（`core.py`）。所有用户提示及界面文字均使用中文。
## 2. 功能模块划分
### 2.1 核心处理模块（`core.py`）
- 使用 Numba 加速的 Floyd‑Steinberg 和 Atkinson 抖动算法，支持 1‑bit（黑白）和 2‑bit（4 级灰度）输出，抖动强度可调（0‑100%）。
- 图像预处理：透明背景填充为白色，可选黑白边自动裁切，横版图片整体旋转（仅对未切割的图片生效）。
- 切割逻辑：
  - 直接切割：将竖版图片按给定比例垂直分割为 2 或 3 部分。
  - 滚动切割：根据重叠比例计算 3 或 5 个部分的垂直范围，相邻部分保留重叠。
- 图像增强：锐化（0‑100%）、对比度（0‑100%）、局部对比度增强 CLAHE（0‑100%），均在灰度化后、抖动前应用。
- 缩放：拉伸至目标分辨率或按比例缩放并居中填充黑边；缩放算法使用 Lanczos 重采样。
- 位深度：支持 1‑16 位输出（实际处理时限制为 1‑8 位，因为 PIL 和抖动算法最高支持 8 位）。
- 编码：根据格式生成 XTG（1‑bit）或 XTH（2‑bit）二进制数据，容器格式（XTC/XTCH）的组装在调用层完成。
- 多进程支持：每张图片（或 GIF 每帧）的处理在独立进程中完成，进程数由用户指定。
### 2.2 控制台版本（`ebook2xtx.py`）
- 启动后依次询问所有转换参数，并提供默认值。
- 支持以下选项：
  - 输出格式（XTC/XTCH/XTG/XTH/图片格式/电子书格式）
  - 图片格式子选项（JPEG/PNG/WebP/BMP，默认 JPEG）
  - 电子书格式子选项（EPUB/PDF）
  - 目标分辨率（预设 X4/X3 及双倍，或自定义宽高，或原分辨率）
  - 自动裁切黑白边（是/否）
  - 横版图片旋转方式（顺时针/逆时针/不旋转）
  - 画面切割模式（不切割/横切2图/横切3图）及其详细子选项（比例、滚动重叠比例）
  - 拉伸至全屏（是/否）
  - 抖动算法（Floyd‑Steinberg/Atkinson/无抖动，默认 Atkinson）
  - 位深度（1‑16，默认根据输出格式自动设置）
  - 抖动强度（0‑100%，默认 70%）
  - 锐化强度（0‑100%，默认 20%）
  - 对比度强度（0‑100%，默认 100%）
  - 局部对比度增强强度（0‑100%，默认 50%）
  - 并发进程数（上限 61）
  - 单页模式或图片格式下的文件名格式（编号/电子书名-编号）
  - 容器模式下的分包大小（4G/自定义/不分包）
  - GIF 动图处理方式（只处理第一帧/处理所有帧/跳过）
- 递归遍历用户指定的目录，查找所有支持格式的输入：
  - 压缩包：`.zip`, `.cbz`, `.7z`, `.cbr`, `.rar`
  - 图片文件夹（子目录名作为电子书名）
  - 容器文件：`.xtc`, `.xtch`
  - 单页文件集合：`.xtg`, `.xth`
  - 电子书：`.epub`, `.mobi`, `.azw3`, `.pdf`
  - 普通图片文件夹（根目录下直接放图片）
- 使用 `ProcessPoolExecutor` 并行处理每个输入项内的图片，实时显示进度。
- 日志记录：在程序目录下创建 `log` 文件夹，每次运行生成带时间戳的日志文件，记录所有操作及错误信息；日志级别默认为 DEBUG，便于调试。
- 解压 RAR 文件时，优先使用程序根目录下的 `UnRAR.exe`（若存在），否则回退到 `rarfile` 库。
- 若检测到缺失的依赖（Pillow、py7zr、rarfile、natsort、numpy、numba、PyMuPDF、ebooklib、BeautifulSoup4、mobi、img2pdf、opencv‑python），自动通过 pip 安装。
- GIF 动图处理：根据用户选择的 `gif_mode` 展开所有帧或跳过。
- 电子书类型自动判断（纯图片/纯文本/图文混排），并提供相应处理建议（纯文本可输出 TXT，图文混排可仅提取图片或仅提取文本）。
### 2.3 图形界面版本（`ebook2xtx_GUI.py`）
- 基于 tkinter 构建，主窗口布局：
  - 顶部：目录设置（输入目录、输出目录，带浏览按钮）
  - 中间：参数选项区，采用双列布局，内容较多时支持垂直滚动
  - 底部：转换进度（总进度条、当前文件进度条）及控制按钮（开始转换、停止、预览、打开日志窗口、关于）
- 所有参数通过图形控件（单选按钮、复选框、输入框、下拉框、滑块等）设置，实时反映动态选项（如分辨率自定义、切割子选项、单页/容器模式下的特殊选项）。
- 提供独立的预览窗口（子模块 `ebook2xtx_gui_viewer.py`），支持打开压缩包、文件夹、EPUB/PDF 等格式的漫画，并实时应用主程序当前设置（旋转、切割、缩放、增强、抖动等）渲染图像。预览窗口支持翻页、缩放、全屏、背景色设置、双页显示（部分）等功能，页面顺序与转换输出完全一致。
- 提供独立的日志窗口，显示程序运行日志，可最大化最小化，默认不自动打开。
- 提供“关于”窗口，显示软件名称、版本号及 GitHub 链接（可点击打开浏览器）。
- 窗口可调整大小，并设置最小尺寸（1000×800）以保证所有控件可见。
- 启动时自动检查并安装缺失依赖（复用控制台版本的依赖检查函数）。
- 转换过程异步执行（线程），通过回调更新进度条和日志窗口，支持停止操作（需等待当前图片处理完成）。
- 日志同时写入文件（与控制台版本共用日志目录）并显示在独立日志窗口中。
## 3. 图像处理流程（控制台与 GUI 一致）
1. 预处理：透明背景填充白色 → 可选黑白边裁切 → 转换为灰度。
2. 整体旋转：若图片为横版且用户选择了旋转方式，则整体旋转（旋转后不再参与切割）。
3. 切割（仅竖版、非首尾页且用户选择切割时执行）：
   - 直接切割：按比例分割成 2 或 3 块。
   - 滚动切割：按重叠比例计算相邻块重叠区域，输出 3 或 5 块。
   - 切割后的每个子图应用用户选择的旋转方式（顺时针/逆时针/不旋转）。
4. 缩放：拉伸至目标分辨率，或按比例缩放后居中填充黑边。
5. 图像增强：依次应用锐化、对比度调整、局部对比度增强（CLAHE）。
6. 抖动与量化：使用用户选择的抖动算法（Floyd‑Steinberg/Atkinson/无抖动）和强度，将图像降低到目标位深度（1‑16 位），然后编码为 XTG/XTH 二进制。
## 4. 输出与文件管理
- **容器格式（XTC/XTCH）**：
  - 将所有图片数据合并为一个容器文件，按用户选择的分包大小拆分为多个文件（如 `电子书名.xtc`、`电子书名-1.xtc`）。
  - 容器头部包含元数据（标题、作者、时间戳）、索引表等；索引表中的宽高从每页的实际数据中提取，避免使用目标分辨率。
- **单页模式（XTG/XTH）**：
  - 在输出目录下创建以电子书名命名的文件夹，内部按用户指定的文件名格式存放单页文件（`1.xtg` 或 `电子书名-1.xtg`）。
- **图片格式（JPEG/PNG/WebP/BMP）**：
  - 在输出目录下创建以电子书名命名的文件夹，内部存放 `0001.jpg`、`0002.jpg` … 或 `电子书名-0001.jpg` 等文件（支持自定义编号格式）。
- **电子书格式（EPUB/PDF）**：
  - 生成 EPUB 时不添加导航目录（`book.toc = []`），直接展示图片内容，避免阅读器显示多余目录页。
  - 生成 PDF 时将每张图片转换为 RGB 并拼接为单个 PDF。
- **文件名清理**：文件名中的非法字符（`<>:"/\|?*`）自动移除，并在日志中记录。
- **日志文件**：每次运行在 `log/` 目录下生成带时间戳的 `.log` 文件，记录详细处理过程；预览窗口单独生成以 `Ebook2XTX_Viewer` 开头的日志文件。
## 5. 依赖与部署
- 所需第三方库：`Pillow`、`py7zr`、`rarfile`、`natsort`、`numpy`、`numba`、`PyMuPDF`、`ebooklib`、`BeautifulSoup4`、`mobi`、`img2pdf`、`opencv‑python`。
- 程序启动时自动检测并安装缺失依赖（通过 pip）。
- 支持通过 PyInstaller 打包为单文件可执行程序，需将 `UnRAR.exe` 嵌入，并在代码中正确处理路径（`sys._MEIPASS`）。
- 日志目录自动创建，日志文件按时间命名，便于排障。
## 6. 使用示例（控制台）
1. 运行 `ebook2xtx.py`。
2. 按提示选择输出格式、分辨率、裁切、旋转、切割、拉伸、抖动算法、位深度、抖动强度、锐化、对比度、CLAHE、并发数、GIF 处理方式等。
3. 输入包含电子书/压缩包/图片文件夹的目录路径和输出目录。
4. 程序自动处理所有输入项，显示进度并输出日志。
5. 处理完成后按回车退出。
## 7. 使用示例（图形界面）
1. 运行 `ebook2xtx_GUI.py`。
2. 在“目录设置”中指定输入和输出目录。
3. 通过各选项卡设置所有转换参数（输出格式、分辨率、切割、抖动算法、增强选项等）。
4. 可选：点击“预览”按钮打开独立预览窗口，加载一本漫画并实时查看当前设置的效果。
5. 点击“开始转换”，观察进度条和独立日志窗口。
6. 转换完成后可查看输出文件，点击“关于”查看版本信息。
```

实现原理参考了bigbag大佬的epub-to-xtc-converter给大佬点赞：https://github.com/bigbag/epub-to-xtc-converter
